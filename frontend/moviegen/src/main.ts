import { createApp } from 'vue';
import App from './App.vue';

// Import your global styles
import './assets/styles.css';

// FontAwesome is linked in index.html, no need to import here

createApp(App).mount('#app');